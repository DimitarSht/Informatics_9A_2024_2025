#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    /// Функция за степенуване
    pow(base, power); -> степенуваното число
    base ^ power
    2 ^ 10

    /// Функция за коренуване
    sqrt(number); -> корен квадратен от number

    /// Функция за модул
    abs(number); -> модул на number

    abs(x - y);

    x < 0 -> x * (-1)








    for(int x = 0; x < 4; x++)
    {
        for(int y = 0; y < 4; y++)
        {
            if(x <= y) cout<<"*";
            else cout<<" ";
        }
        cout<<endl;
    }
    return 0;
}

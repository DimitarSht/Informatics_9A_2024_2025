#include<iostream> /// cin, cout
#include<cmath>     /// математика
using namespace std;
int main()
{
    /*
    int - цяло число
    char - символ
    double - число с дес. запетая
    float - число с дес. запетая
    bool - 0 или 1
    1 = true
    0 = false
    int a;

    if(a > 10)
    {
        cout<<a;
    }
    else if(a < 10)
    {
        cout<<a--;
    }
    else
    {}
    a+=2; <-> a = a + 2
    10 / 3 -> 3
    10 % 3 -> 1

    int num, MIN = 10000000, MAX = -10000000;
    if(num < MIN) MIN = num;
    if(num > MAX) MAX = num;


    int num, MIN = 10000000, MAX = -10000000;
    cin>>num;
    for(int i = 0; i < 10; i++)
    {
        if(num < MIN) MIN = num;
        if(num > MAX) MAX = num;
        cin>>num;
    }
    cout<<MIN<<" "<<MAX;

    if(num % 2 == 0) /// проверка за четност

    for(int i = 0 ; num != 0 ; i++) /// намеране сума на цифрите на число
    {
        sum += num % 10; <-> sum = sum + num % 10
        num /= 10;
    }
    */
    return 0;
}
